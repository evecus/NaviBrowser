<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="8dp"
        android:paddingBottom="32dp">

        <LinearLayout style="@style/SettingsRow">
            <TextView style="@style/SettingsRowTitle" android:text="@string/quick_switch_enable" />
            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/switchQuickSwitchEnabled"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>
        <include layout="@layout/item_divider" />

        <TextView
            android:id="@+id/tvSectionTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/quick_switch_engine_list"
            android:textSize="13sp"
            android:textColor="@color/textSecondary"
            android:paddingLeft="20dp"
            android:paddingRight="20dp"
            android:paddingTop="16dp"
            android:paddingBottom="8dp" />

        <LinearLayout
            android:id="@+id/engineListContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <LinearLayout
            android:id="@+id/btnAddEngine"
            style="@style/SettingsRow.Clickable"
            android:gravity="center"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">
            <TextView
                style="@style/SettingsRowTitle"
                android:gravity="center"
                android:text="@string/quick_switch_add_engine" />
        </LinearLayout>
        <include layout="@layout/item_divider" />

        <LinearLayout
            android:id="@+id/btnResetDefaults"
            style="@style/SettingsRow.Clickable"
            android:gravity="center"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">
            <TextView
                style="@style/SettingsRowTitle"
                android:gravity="center"
                android:textColor="@color/danger"
                android:text="@string/quick_switch_reset_defaults" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
