<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaviBrowser"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.browser.BrowserActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Settings full-screen activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.browser.BrowserActivity" />

        <!-- Settings Category detail activity -->
        <activity
            android:name=".ui.settings.SettingsCategoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- User Script Manager -->
        <activity
            android:name=".ui.settings.ScriptManagerActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- Download Manager full-screen activity -->
        <activity
            android:name=".ui.download.DownloadManagerActivity"
            android:exported="false"
            android:parentActivityName=".ui.browser.BrowserActivity" />

        <!-- Password Manager full-screen activity -->
        <activity
            android:name=".ui.password.PasswordManagerActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <service
            android:name=".ui.download.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
